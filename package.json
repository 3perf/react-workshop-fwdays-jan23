{
  "name": "commerce",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "site",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "start": "concurrently --names commerce,commerce-local,site 'cd packages/commerce && yarn dev' 'cd packages/local && yarn dev' 'sleep 5 && cd site && yarn dev'",
    "serve": "cd site && yarn start"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "prettier": "^2.7.1"
  }
}
